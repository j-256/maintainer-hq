// Volume scenarios exercise repeated domain operations against real D1 storage
// Allow for shared CI runner latency without changing application deadlines
export const D1_VOLUME_TEST_TIMEOUT_MS = 30_000;
